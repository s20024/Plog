---
// 仮のデータ（実際の実装ではMDXファイルからデータを取得します）
const featuredPosts = [
  {
    id: 1,
    title: "Astroでブログサイトを作る方法",
    excerpt:
      "Astroを使って高速なブログサイトを構築する方法を解説します。初心者でも簡単に始められるチュートリアル。",
    date: "2025-03-15",
    slug: "how-to-build-blog-with-astro",
    category: "開発",
    image: "/placeholder-1.jpg",
  },
  {
    id: 2,
    title: "Reactコンポーネントの最適化テクニック",
    excerpt:
      "Reactアプリケーションのパフォーマンスを向上させるための実践的なテクニックを紹介します。",
    date: "2025-03-10",
    slug: "react-optimization-techniques",
    category: "React",
    image: "/placeholder-2.jpg",
  },
  {
    id: 3,
    title: "SCSSで効率的なスタイリング",
    excerpt:
      "SCSSの変数、ミックスイン、ネスティングを活用して、メンテナンス性の高いCSSを書く方法。",
    date: "2025-03-05",
    slug: "efficient-styling-with-scss",
    category: "CSS",
    image: "/placeholder-3.jpg",
  },
];
---

<div class="featured-posts">
  {
    featuredPosts.map((post) => (
      <article class="post-card">
        <div class="post-image">
          <div
            class="image-placeholder"
            style={`background-color: ${["var(--base-color)", "var(--sub-color-1)", "var(--sub-color-3)"][Math.floor(Math.random() * 3)]}`}
          >
            <span class="category-tag">{post.category}</span>
          </div>
        </div>
        <div class="post-content">
          <h3 class="post-title">
            <a href={`/blog/${post.slug}`}>{post.title}</a>
          </h3>
          <p class="post-date">
            {new Date(post.date).toLocaleDateString("ja-JP", {
              year: "numeric",
              month: "long",
              day: "numeric",
            })}
          </p>
          <p class="post-excerpt">{post.excerpt}</p>
          <a href={`/blog/${post.slug}`} class="read-more">
            続きを読む →
          </a>
        </div>
      </article>
    ))
  }
</div>

<style lang="scss">
  .featured-posts {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
  }

  .post-card {
    background-color: white;
    border-radius: 16px;
    overflow: hidden;
    box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
    transition:
      transform 0.3s ease,
      box-shadow 0.3s ease;

    &:hover {
      transform: translateY(-5px);
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
    }
  }

  .post-image {
    height: 200px;
    overflow: hidden;

    .image-placeholder {
      width: 100%;
      height: 100%;
      display: flex;
      align-items: flex-start;
      justify-content: flex-end;
      padding: 1rem;
      position: relative;
    }

    .category-tag {
      background-color: white;
      color: var(--text-color);
      font-size: 0.8rem;
      font-weight: 600;
      padding: 0.3rem 0.8rem;
      border-radius: 50px;
    }
  }

  .post-content {
    padding: 1.5rem;
  }

  .post-title {
    font-size: 1.3rem;
    margin: 0 0 0.5rem;
    line-height: 1.4;

    a {
      color: var(--text-color);
      text-decoration: none;

      &:hover {
        color: var(--base-color);
      }
    }
  }

  .post-date {
    font-size: 0.85rem;
    color: #666;
    margin-bottom: 1rem;
  }

  .post-excerpt {
    font-size: 0.95rem;
    margin-bottom: 1.5rem;
    line-height: 1.6;
    display: -webkit-box;
    -webkit-line-clamp: 3;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .read-more {
    font-size: 0.9rem;
    font-weight: 600;
    color: var(--text-color);

    &:hover {
      color: var(--base-color);
    }
  }

  @media (max-width: 992px) {
    .featured-posts {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 768px) {
    .featured-posts {
      grid-template-columns: 1fr;
    }
  }
</style>
